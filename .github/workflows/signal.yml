name: Options Signal
on:
  schedule:
    - cron: "0 4 * * 1-5"
    - cron: "30 6 * * 1-5"
    - cron: "0 9 * * 1-5"
  workflow_dispatch:
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: pip install requests yfinance
      - run: python bot.py
